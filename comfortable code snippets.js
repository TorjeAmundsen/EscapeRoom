/*

Instruksjon: Putt # foran det du ikke er konfortabl med.

    div
    buttons
    if-setninger
    for loops med index
    for-of loops igjennom liste
    å kalle bilder fra internett
    å kalle bilder fra lokalt
    å bruke ternary ( : ? ) ? = truthy, : = falsy...
    while
    getElementsByClass
    button disable
    querySelector
    return-verdier
    multiline-strings med variabler ( `example ${test} more stuff` )
    spread-operator ( let newArray = [...array] )
    
    
    ##eval
    ###arrow-functions
    ### JS Classes
*/
for (let index = 0; index < array.length; index++) {}

for (bil of biler) {
}

document.getElementById("app");

document.querySelector("#app"); // id
document.querySelector(".right-box"); // class
document.querySelectorAll("button"); // element-type
document.querySelectorAll(".right-box", ".left-box"); // velg flere typer ting samtidig
